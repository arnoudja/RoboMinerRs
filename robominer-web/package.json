{
  "name": "robominer-web-static",
  "private": true,
  "description": "Dev-only lint tooling for robominer-web/static JS (no runtime deps).",
  "scripts": {
    "lint": "eslint static/js --max-warnings 0"
  },
  "devDependencies": {
    "eslint": "^9.22.0"
  }
}
